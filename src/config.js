// Public site configuration.
//
// These two values are meant to be public — they end up visible in the page
// source — so it is safe to commit them for a static site on GitHub Pages.
// Leave a value empty ('') to disable that feature; nothing is injected until
// you fill it in.

// Google Analytics 4 — the "Measurement ID" from your GA4 property.
// Admin → Data streams → your web stream → Measurement ID. Looks like 'G-XXXXXXXXXX'.
export const GA_ID = 'G-YYML8FT8E0';

// Google Search Console — the token from the "HTML tag" verification method.
// Search Console → Settings → Ownership verification → HTML tag: copy ONLY the
// value of content="..." (not the whole meta tag). You can also verify by DNS
// instead, in which case leave this empty.
export const GOOGLE_SITE_VERIFICATION = '';
